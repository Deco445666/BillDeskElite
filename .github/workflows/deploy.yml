name: Trigger Expo Cloud Build (With Credentials)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  eas-build:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Setup Repo
        uses: actions/checkout@v3

      - name: 🏗 Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: 📦 Install EAS CLI
        run: npm install -g eas-cli

      # --- THE FIX STARTS HERE ---
      - name: 🔐 Generate & Upload Keystore
        env:
          # Your Expo Token is hardcoded here for reliability
          EXPO_TOKEN: MbuueX4TP3U7uHzmBVjwWmE7nBy4UE0m9qy_n56A
        run: |
          # 1. Create a Keystore File
          echo "Generating Android Keystore..."
          keytool -genkey -v -keystore release.keystore -alias androidreleasekey -keyalg RSA -keysize 2048 -validity 10000 -storepass android -keypass android -dname "CN=BillDesk,O=BillDesk,C=US"
          
          # 2. Upload it to Expo (This prevents the 'Interactive' error)
          echo "Uploading to Expo..."
          # We use '|| true' so if credentials already exist, it doesn't crash
          eas credentials:push android \
            --keystore-path release.keystore \
            --keystore-password android \
            --key-alias androidreleasekey \
            --key-password android \
            --non-interactive || true

      # --- BUILD STEP ---
      - name: 🚀 Trigger Build
        env:
          EXPO_TOKEN: MbuueX4TP3U7uHzmBVjwWmE7nBy4UE0m9qy_n56A
        run: eas build --platform android --profile preview --non-interactive --no-wait


