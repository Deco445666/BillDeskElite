name: Build Final Production APK (Separated Steps)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Setup Environment
      - name: 🏗 Setup Repo
        uses: actions/checkout@v3

      - name: 🏗 Setup Java 17
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: 🏗 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      # 2. Install Dependencies
      - name: 📦 Install Dependencies
        run: npm install

      - name: 📦 Install Expo CLI
        run: npm install -g expo-cli

      # 3. Generate Valid Assets
      - name: 🖼️ Generate Valid Assets
        run: |
          sudo apt-get update
          sudo apt-get install -y imagemagick
          mkdir -p assets
          convert -size 1024x1024 xc:blue assets/icon.png
          convert -size 1242x2436 xc:blue assets/splash.png
          cp assets/icon.png assets/adaptive-icon.png
          cp assets/icon.png assets/favicon.png

      # 4. Prebuild (Fresh Android Folder)
      - name: ⚙️ Prebuild
        run: npx expo prebuild --platform android --clean

      # ---------------------------------------------------------
      # STEP 5: FIX SETTINGS FILE (gradle.properties)
      # This handles Memory, Flipper, and Architecture (OnePlus)
      # ---------------------------------------------------------
      - name: 🔧 Fix Gradle Properties
        working-directory: android
        run: |
          # Remove bad flags
          if [ -f gradle.properties ]; then
              sed -i '/android.enableR8/d' gradle.properties
          fi
          
          # Append Safe Settings
          echo "" >> gradle.properties
          echo "# MEMORY & ARCHITECTURE" >> gradle.properties
          echo "reactNativeArchitectures=arm64-v8a" >> gradle.properties
          echo "FLIPPER_VERSION=" >> gradle.properties
          echo "useFlipper=false" >> gradle.properties
          echo "hermesEnabled=true" >> gradle.properties
          
          # CRITICAL MEMORY CAP (1.5GB)
          echo "org.gradle.jvmargs=-Xmx1536m -XX:MaxMetaspaceSize=512m" >> gradle.properties
          echo "org.gradle.daemon=false" >> gradle.properties
          echo "org.gradle.parallel=false" >> gradle.properties
          echo "org.gradle.caching=false" >> gradle.properties
          echo "kotlin.compiler.execution.strategy=in-process" >> gradle.properties

      # ---------------------------------------------------------
      # STEP 6: FIX BUILD CODE (build.gradle)
      # This handles SDK Versions and Library Conflicts
      # ---------------------------------------------------------
      - name: 🔧 Fix Build Gradle
        working-directory: android
        run: |
          # A. Regex Patch for SDK 34
          find . -name "build.gradle" -print0 | xargs -0 sed -i -E 's/compileSdkVersion\s*=?\s*[0-9]+/compileSdkVersion = 34/g' || true
          find . -name "build.gradle" -print0 | xargs -0 sed -i -E 's/targetSdkVersion\s*=?\s*[0-9]+/targetSdkVersion = 34/g' || true
          
          # B. Inject Dependency Locks (Force older compatible libraries)
          cat <<EOF >> build.gradle
          allprojects {
              configurations.all {
                  resolutionStrategy {
                      force 'androidx.webkit:webkit:1.7.0'
                      force 'androidx.annotation:annotation-experimental:1.3.1'
                  }
              }
              tasks.withType(org.jetbrains.kotlin.gradle.tasks.KotlinCompile).configureEach {
                  kotlinOptions { jvmTarget = "17" }
              }
          }
          EOF

      # 7. Generate Keystore
      - name: 🔐 Generate Keystore
        run: |
          keytool -genkey -v -keystore release.keystore -alias androidreleasekey -keyalg RSA -keysize 2048 -validity 10000 -storepass android -keypass android -dname "CN=Android Debug,O=Android,C=US"

      # 8. Build Release APK (With Node Memory Boost)
      - name: 🔨 Build Release APK
        working-directory: android
        env:
          # Node gets 3.5GB RAM
          NODE_OPTIONS: "--max_old_space_size=3584"
        run: |
          chmod +x gradlew
          ./gradlew clean
          ./gradlew assembleRelease \
            -Pandroid.injected.signing.store.file=$(pwd)/../release.keystore \
            -Pandroid.injected.signing.store.password=android \
            -Pandroid.injected.signing.key.alias=androidreleasekey \
            -Pandroid.injected.signing.key.password=android \
            -Pandroid.enableProguardInReleaseBuilds=false \
            -Pandroid.shrinkResources=false \
            --no-daemon \
            --max-workers=1

      # 9. Upload Result
      - name: 📤 Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: BillDesk-Final-Separated
          path: android/app/build/outputs/apk/release/app-release.apk
