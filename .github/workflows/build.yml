name: Build Android APK

on:
  workflow_dispatch: {}
  push:
    branches:
      - main

jobs:
  build:
    name: EAS Build
    runs-on: ubuntu-latest
    env:
      _JAVA_OPTIONS: "-Xmx4096m -XX:+UseParallelGC"
    steps:
      - name: 🏗 Setup Repo
        uses: actions/checkout@v3

      - name: 🏗 Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18.x

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          packager: npm
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 🧹 Nuclear Cache Clean & File Guard
        run: |
          npm cache clean --force
          rm -rf node_modules
          # FORCE DELETE any conflicting JS configs that might be lingering
          rm -f babel.config.js
          rm -f app.config.js
          rm -f app.config.ts
          echo "Conflicting config files deleted."

      - name: 📦 Install Dependencies
        run: npm install

      - name: 🚀 Build APK
        run: eas build --platform android --profile preview --non-interactive --local --clear-cache

      - name: 📤 Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: "*.apk"
